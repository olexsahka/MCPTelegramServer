<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <!-- Mute noisy TDLib internal logs -->
        <logger name="it.tdlight.TDLight" level="WARN"/>
    </springProfile>

    <springProfile name="prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{ISO8601} %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
        <!-- Allow our app logs at INFO, but nothing sensitive -->
        <logger name="org.example.mcptelegram" level="INFO"/>
        <!-- Silence TDLib completely in prod -->
        <logger name="it.tdlight" level="ERROR"/>
        <!-- Silence Hibernate SQL -->
        <logger name="org.hibernate.SQL" level="ERROR"/>
        <!-- Silence Spring Security details -->
        <logger name="org.springframework.security" level="WARN"/>
    </springProfile>
</configuration>
